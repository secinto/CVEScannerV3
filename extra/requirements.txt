# Minimum versions known to work on Python 3.8–3.14.
# Use >= (not ==) so a newer interpreter can resolve compatible wheels:
# exact pins previously broke on Python 3.14 (httpcore==1.0.4 crashed at import).
anyio>=4.3.0
certifi>=2024.2.2
exceptiongroup>=1.2.0
fake-useragent>=1.5.1
h11>=0.16.0
httpcore>=1.0.9
httpx>=0.28.1
idna>=3.6
pyrate-limiter>=2.10.0
sniffio>=1.3.1
texttable>=1.7.0
tqdm>=4.66.2
typing_extensions>=4.10.0
